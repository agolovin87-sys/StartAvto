/**
 * Дополнительные точки входа для типов. Основной модуль приложения — `src/types.ts`.
 */
export type { PasskeyRegistration } from "../types";
export type {
  ScheduleExportInstructor,
  ScheduleGrid,
  ScheduleLesson,
  ScheduleWeekRange,
} from "./schedule";
